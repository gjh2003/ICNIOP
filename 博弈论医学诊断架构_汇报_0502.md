# 成本非对称专家路由 —— 长尾骨髓细胞分类汇报（v3, 0502）

> **本版本与 0422 版本的区别**：
> - 创新点重新定位：从"竞标博弈框架"改为"**成本非对称的专家竞标路由**"
> - Nash 均衡分析降级为"理论支撑"，避免审稿人攻击闭式解未被强制约束
> - 加入与 RIDE / SADE / MoB（2024.12）的本质区别说明
> - 加入"医学会诊机制"作为核心 motivation
> - 完善基线列表（GALA 2024、SADE NeurIPS 2024）和消融实验设计（含可学习成本）

## 一、上次汇报后的工作进展

上次汇报（3.25）提出了非对称纳什竞标博弈的理论框架，推导了阶级固化出价税和纳什均衡解。本阶段完成了**完整的实验验证和方法改进**。

---

## 二、实验验证（发现了原方案的问题）

### 2.1 纳什出价税的初始实验

按照上次汇报的理论公式，损失函数为：

$$\mathcal{L} = -\log p_y + \frac{\tau}{2} \sum_{k=1}^{K} \pi_k \cdot b_k^2$$

在 MLL 数据集（171,374 张图像，21 个造血谱系类别，最大/最小类比 3678:1）上进行了两轮实验。

**实验一：冻结 ResNet50 特征 + Nash 出价税**

| 方法                  | Accuracy | Macro F1 |
| --------------------- | -------- | -------- |
| Standard CE           | 68.60%   | 0.388    |
| Nash 出价税 (τ=1.0)   | 67.37%   | 0.374    |
| Nash 出价税 (τ=0.001) | 68.60%   | 0.397    |

**发现的问题**：

1. τ=1.0 时**反而降低性能**，对大类惩罚过重导致整体崩溃
2. 所有方法天花板仅 ~68%，ImageNet 预训练特征对骨髓细胞域适配性差
3. 通过 τ 消融实验（0.001~10.0 共 11 个值），发现 τ=0.001 最优，但提升仅 +2.3%

**实验二：端到端微调 ResNet50**

将骨干网络改为端到端训练，结果大幅提升：

| 方法                  | Accuracy | Macro F1 | Weighted F1 | Head F1 | Tail F1 |
| --------------------- | -------- | -------- | ----------- | ------- | ------- |
| Standard CE           | 85.98%   | 0.607    | 0.858       | 0.911   | 0.231   |
| Weighted CE           | 83.21%   | 0.616    | 0.839       | 0.890   | 0.294   |
| Focal Loss            | 72.45%   | 0.517    | 0.751       | 0.805   | 0.183   |
| Nash 出价税 (τ=0.001) | 86.14%   | 0.612    | 0.859       | 0.912   | 0.246   |

**关键发现**：

- 端到端微调带来了 **+17.5% 的准确率提升**（68%→86%），说明特征适配是核心瓶颈
- Nash 出价税的 Accuracy 和 Head F1 最优，但 **Macro F1 和 Tail F1 没有赢 Weighted CE**

### 2.2 原方案问题的诊断

经过理论分析和文献调研，确认原纳什出价税 $\frac{\tau}{2}\sum \pi_k b_k^2$ 存在**结构性缺陷**：

1. **本质上是 L2 正则化**：数学上等价于按类频率加权的 logit L2 惩罚，不是真正的博弈
2. **没有策略互依性**：缺乏多个玩家之间的交互，不满足博弈论的基本定义
3. **没有均衡求解过程**：softmax 路由不是拍卖机制，无法形成纳什均衡
4. **单一损失函数能力有限**：无论怎么调 τ，都无法同时优化头部和尾部

---

## 三、第一次改进：三阶段 Nash 框架（v1）

基于文献调研（Decoupled Training [ICLR 2020]、Logit Adjustment [ICLR 2021]、SADE [NeurIPS 2024]），设计了三阶段框架，并将骨干网络升级为 **Swin-Tiny**（28M 参数）：

- **Stage 1**：解耦表征学习（标准 CE 端到端训练 Swin-Tiny，然后冻结骨干）
- **Stage 2**：Nash 均衡分类器（冻结骨干，Logit Adjustment + Nash 出价税训练新分类头）
- **Stage 3**：多专家路由（3 个不同损失函数训练的专家 + 学习的门控路由网络）

### 3.1 v1 实验结果（Swin-Tiny 骨干）

| 阶段                     | Accuracy   | Macro F1  | Weighted F1 | Head F1   | Tail F1   |
| ------------------------ | ---------- | --------- | ----------- | --------- | --------- |
| S1: CE Baseline          | 86.18%     | 0.623     | 0.860       | 0.913     | 0.281     |
| S2: LA + Nash Tax        | 83.53%     | 0.615     | 0.847       | 0.899     | 0.277     |
| S3: Multi-Expert Routing | **86.83%** | **0.682** | **0.867**   | **0.917** | **0.436** |

### 3.2 v1 发现的问题

1. **S2 反而比 S1 退步了**（Macro F1 从 0.623 降到 0.615）。原因是 Logit Adjustment 的 τ=1.0 过于激进，且 S2 丢弃了 S1 训好的分类头从零开始训练
2. **S2 和 S3 实际上是并行的**（都从 S1 的冻结骨干出发，各自独立训练），不是真正的串行三阶段
3. **路由网络是一个普通的门控网络**，缺乏博弈论的理论基础——和 RIDE (ICLR 2021)、SADE (NeurIPS 2024) 等已有方法没有本质区别
4. **Nash 出价税依然是 L2 正则化的包装**，不是真正的博弈机制

---

## 四、第二次改进：成本非对称的专家竞标路由（v2）

### 4.1 动机：从医学会诊机制重新审视多专家路由

**关键观察**：现有多专家长尾分类方法（RIDE [ICLR 2021], SADE [NeurIPS 2022]）都隐含一个假设——**所有专家是对称的、地位相同的**，路由网络仅根据样本特征"公平地"在专家间分配权重。

这与真实医学诊断场景脱节。在真实的多专家会诊中：

- **全科医生**首诊大多数病例，便宜可靠（低成本）
- **专科医生**仅在疑难病例时介入，相对专业但调用代价更高（中成本）
- **顶级专家/罕见病专家**只对极端样本出诊，最稀缺最专业（高成本）

这种**专家地位的非对称性**——本应是医学决策系统的核心特征——在现有方法中完全缺失。

### 4.2 核心创新：非对称成本下的竞标路由机制

我们提出**让每个专家显式地拥有不同的"出价成本"**，将路由问题建模为成本非对称的竞标博弈：

$$U_i(b_i, \mathbf{b}_{-i}) = \underbrace{w_i(\mathbf{b}) \cdot r_i(x, y)}_{\text{获得的署名权 × 自身能力}} - \underbrace{c_i \cdot b_i^2}_{\text{出价成本（专家越稀缺越高）}}$$

其中 $c_1 < c_2 < c_3$（头部专家成本低、尾部专家成本高）显式编码了**专家的领域优先级**。

**这一机制带来三个其他方法没有的性质**（后文实验证明）：

1. **领域优先级编码**：低成本专家（A）默认参与大多数决策，高成本专家（C）只在样本特征召唤它时才介入
2. **自适应退让**：当某专家已主导（$w_i \to 1$）时，softmax 导数 $w_i(1-w_i) \to 0$ 自动让路给其他专家，**防止专家垄断**
3. **可解释的决策路径**：通过观察各样本的出价分布，可以解释"哪个专家做出了主要诊断"——这是医学落地的关键

### 4.3 与现有方法的本质区别

|              | RIDE [ICLR 21]       | SADE [NeurIPS 22]                  | MoB [ArXiv 24]               | **Ours**                            |
| ------------ | -------------------- | ---------------------------------- | ---------------------------- | ------------------------------------ |
| 路由方式     | 学习门控（softmax）   | 测试时自监督聚合                    | VCG 拍卖（硬选择单专家）       | **成本非对称竞标（软聚合）**          |
| 专家地位     | 对称                  | 对称                                | 对称（出价反映自身真实成本）   | **非对称（外生设定 $c_i$ 编码先验）** |
| 应用场景     | 通用长尾              | 测试分布未知                        | 持续学习（避免遗忘）          | **长尾医学分类（专家专精性）**         |
| 多样性来源   | KL 散度损失           | 不同分布训练                        | 任务历史                     | **不同损失 + 不同采样**               |
| 主要损失     | $CE + \lambda \cdot KL$ | 各 expert 独立 CE                  | VCG 选择                     | $CE + \alpha \cdot \sum (-U_i)$       |

**与 MoB（最近相关工作）的关键差异**：MoB 处理持续学习场景，专家诚实出价反映"加入此任务的真实成本"，使用 VCG 机制硬选择单个专家以避免遗忘。我们处理静态长尾分类，**外生地**为专家分配成本以编码领域先验，软聚合所有专家以利用互补信号。两个方法是为不同问题设计的不同机制。

### 4.4 v1 → v2 的设计演进

|            | v1（三阶段路由）           | v2（竞标博弈）                    |
| ---------- | -------------------------- | --------------------------------- |
| 博弈结构   | 门控网络（和 RIDE 类似）   | 多专家显式竞标，有效用函数        |
| 专家交互   | 无交互（路由独立决定权重） | 一个专家出价高 → 其他权重自动降低 |
| 理论保证   | 无                         | 均衡存在性定理 + 闭式解           |
| 可训练部分 | 路由网络                   | 每个专家的独立竞标网络            |

### 4.5 方法设计

整体分为三个 Phase：**表征学习 → 专家预训练 → 竞标博弈**。

#### Phase 1：解耦表征学习

- 使用 Swin-Tiny（28M 参数）作为骨干网络
- 标准 CE 端到端训练 5 个 epoch，让骨干适配骨髓细胞特征空间
- 训练完成后**冻结骨干网络**

#### Phase 2：专家预训练

冻结骨干网络后，独立训练 3 个分类头（"专家"）。三个专家使用**相同的训练数据（全部 21 类）**，区别在于损失函数和采样策略不同，从而形成不同的决策偏好：

**Expert A — 头部专精（全科医生）**

- 损失函数：标准 CrossEntropy
- 采样方式：自然分布（不做任何重采样）
- 设计意图：CE 在自然分布下训练，梯度由大类主导，模型自然偏向高频类。就像全科医生，见多了常见病，对常见细胞类型（NGS、EBO、LYT 等）的识别最准确
- 测试集表现：Macro F1 = **0.652**，Head F1 = 0.915，Tail F1 = 0.366
- 特点：**整体最强的单一专家**，头部类准确率极高，但 LYI、FGC 两个极端稀有类完全无法识别（F1=0）

**Expert B — 尾部专精（专科医生）**

- 损失函数：加权 CrossEntropy（权重 = 类频率的倒数 $1/\pi_k$）
- 采样方式：均衡采样（WeightedRandomSampler，每个类被采到的概率相同）
- 设计意图：通过过采样稀有类 + 加权损失的双重机制，强制模型关注低频类别
- 测试集表现：Macro F1 = **0.568**，Head F1 = 0.831，Tail F1 = 0.286
- 特点：**单独表现最差**（均衡采样导致过拟合），但它的错误模式与 Expert A 截然不同——Expert A 漏诊尾部类（recall 低），Expert B 过度诊断尾部类（precision 低但 recall 极高）。在集成中，这种差异化的错误模式提供了互补信号

**Expert C — 极端尾部专精（罕见病专家）**

- 损失函数：Logit Adjustment（$\text{logit}_k' = \text{logit}_k + \tau_{LA} \cdot \log(\pi_k)$）
- 采样方式：自然分布
- 设计意图：在 logit 上加 $\log(\pi_k)$ 偏移，给稀有类天然的"起跑优势"——降低判定稀有类的门槛
- 测试集表现：Macro F1 = **0.618**，Head F1 = 0.899，Tail F1 = 0.282
- 特点：介于 A 和 B 之间，对尾部类更敏感但不至于像 B 那样过拟合

**三个专家的互补性（测试集逐类数据）**：

| 尾部类 | 样本数 | Expert A | Expert B | Expert C | 互补分析               |
| ------ | ------ | -------- | -------- | -------- | ---------------------- |
| LYI    | 10     | F1=0.00  | F1=0.34  | F1=0.11  | A 完全漏诊，B/C 有信号 |
| FGC    | 7      | F1=0.00  | F1=0.31  | F1=0.16  | A 完全漏诊，B/C 有信号 |
| KSC    | 6      | F1=0.40  | F1=0.32  | F1=0.16  | 三者各有部分能力       |
| HAC    | 61     | F1=0.72  | F1=0.41  | F1=0.51  | A 最强                 |
| OTH    | 44     | F1=0.76  | F1=0.45  | F1=0.62  | A 最强                 |
| BAS    | 66     | F1=0.68  | F1=0.17  | F1=0.41  | A 最强，B 反而最差     |

关键洞察：**没有一个专家能同时做好所有类别**。Expert A 对 LYI/FGC 完全失灵（F1=0），Expert B 虽然整体弱但对这两个类有信号（0.34/0.31），Expert C 提供了中间立场。博弈机制的价值是**根据每个输入样本的特征，自适应地组合三个专家的优势**。

#### Phase 3：成本非对称竞标路由（核心贡献）

冻结骨干网络和三个专家分类器，为每个专家添加独立的**竞标网络** $g_i$（可训练），三个专家通过非对称成本竞标来分配每个样本的分类权重。

**架构**：

- **3 个专家** $\{E_1, E_2, E_3\}$（来自 Phase 2，参数冻结）
- **3 个竞标网络** $g_i$（仅它们可训练）
- **竞标过程**：$b_i(x) = g_i(\text{features}(x)) \in \mathbb{R}$
- **分配机制**：$w_i = \text{softmax}([b_1, b_2, b_3])_i$
- **集成预测**：$p_{ensemble} = \sum_i w_i \cdot \text{softmax}(f_i(x))$

**关键设计：非对称成本机制**

每个专家的效用函数为：

$$U_i(b_i, \mathbf{b}_{-i}) = w_i(\mathbf{b}) \cdot r_i(x, y) - c_i \cdot b_i^2$$

其中 $r_i(x,y) = \text{softmax}(f_i(x))_y$ 是专家对真实类别的预测概率（**自身能力**），$c_i$ 是预先设定的专家特定成本（**领域优先级**）。

**成本设计——医学会诊的形式化**：

| 专家               | 成本 $c_i$ | 角色类比      | 设计逻辑                                       |
| ------------------ | ---------- | ------------- | ---------------------------------------------- |
| Expert A（全科）   | 0.1（低）  | 全科医生      | 默认参与，覆盖大多数常见病例（头部类）         |
| Expert B（专科）   | 0.5（中）  | 专科医生      | 仅在有把握时介入，避免过度诊断（中部类）       |
| Expert C（罕见病） | 1.0（高）  | 罕见病专家    | 最稀缺资源，只对极端疑难样本出诊（极端尾部）   |

这种**外生分配的非对称成本**显式编码了医学领域的专家优先级——这是 RIDE / SADE 等对称式方法无法表达的。

**训练损失**：

$$\mathcal{L} = \underbrace{CE(p_{ensemble}, y)}_{\text{集成准确性}} + \alpha \cdot \underbrace{\left(-\sum_i U_i\right)}_{\text{成本约束}}$$

仅竞标网络 $g_i$ 参数可训练。$\alpha$ 控制成本机制的强度（$\alpha=0$ 退化为标准 CE 门控，等价于 RIDE 范式）。

#### 4.5.1 理论分析（支撑性）

为说明上述损失设计的合理性，可分析其纳什均衡。固定 $\mathbf{b}_{-i}$ 求 $\partial U_i / \partial b_i = 0$，得到闭式解：

$$b_i^* = \frac{w_i^*(1-w_i^*) \cdot r_i}{2c_i}$$

**该均衡解的三个性质恰好对应我们的设计目标**：

1. **奖励正比** $b_i^* \propto r_i$：能力强的专家在该样本上出价更高 ✓
2. **成本反比** $b_i^* \propto 1/c_i$：$c_1=0.1$ 的全科专家在均衡下出价是 $c_3=1.0$ 罕见病专家的 10 倍——默认处理大多数案例 ✓
3. **自适应退让** $b_i^* \propto w_i(1-w_i)$：已主导的专家会自动降价让位，防止单一专家垄断所有样本 ✓

**均衡存在性**：由 $U_i$ 关于 $b_i$ 严格凹（$\partial^2 U_i / \partial b_i^2 = -2c_i < 0$）+ 策略空间紧致，根据 Debreu-Glicksberg-Fan 定理，纳什均衡存在且唯一。

> **说明**：上述闭式解未在训练中作为硬约束强制执行；它作为**理论解释**说明我们的损失设计是有原则的，神经网络在训练中隐式逼近这一均衡。后续实验（出价分布的可视化）将验证训练后的出价确实符合上述性质。

---

## 五、v2 实验结果

### 5.1 主实验（Swin-Tiny 骨干，测试集）

以下所有数据均为**测试集**上的结果：

| 方法                     | Accuracy   | Macro F1  | Weighted F1 | Head F1   | Tail F1   |
| ------------------------ | ---------- | --------- | ----------- | --------- | --------- |
| P1: CE Baseline (5 ep)   | 86.33%     | 0.623     | 0.861       | 0.914     | 0.282     |
| Expert A 单独 (10 ep)    | 86.44%     | 0.652     | 0.862       | 0.915     | 0.366     |
| Expert B 单独 (10 ep)    | 74.79%     | 0.568     | 0.779       | 0.831     | 0.286     |
| Expert C 单独 (10 ep)    | 83.82%     | 0.618     | 0.849       | 0.899     | 0.282     |
| **竞标博弈集成（Ours）** | **86.01%** | **0.715** | **0.863**   | **0.913** | **0.545** |

**提升分析**：

- 相比最强单专家 Expert A：**Macro F1 +6.3%**（0.652 → 0.715），**Tail F1 +17.9%**（0.366 → 0.545）
- Head F1 基本持平（0.913 vs 0.915），**没有牺牲头部性能**
- 博弈集成超过了所有单一专家，证明了多专家互补效应的存在

**关于 Expert B 的说明**：Expert B 单独的 Macro F1（0.568）低于 Expert A（0.652），但它在尾部类上的 recall 极高（尾部类平均 recall=0.82 vs Expert A 的 0.38）。Expert A 的问题是漏诊（precision 高但 recall 低），Expert B 的问题是误报（recall 高但 precision 低）。博弈机制利用这种互补性：当 Expert A 对某个尾部样本不确定时，Expert B 的高 recall 信号被融合进来，最终在维持 precision 的同时提升了 recall。

### 5.2 尾部类的逐类分析（测试集）

| 尾部类 | 样本数 | Expert A | Expert B | Expert C | 竞标博弈 | 博弈的提升来源                    |
| ------ | ------ | -------- | -------- | -------- | -------- | --------------------------------- |
| LYI    | 10     | 0.00     | 0.34     | 0.11     | **0.60** | A 完全失灵，博弈融合了 B/C 的信号 |
| FGC    | 7      | 0.00     | 0.31     | 0.16     | **0.60** | 同上                              |
| KSC    | 6      | 0.40     | 0.32     | 0.16     | **0.57** | 三者互补，博弈取长补短            |
| OTH    | 44     | 0.76     | 0.45     | 0.62     | **0.75** | A 已强，博弈维持                  |
| HAC    | 61     | 0.72     | 0.41     | 0.51     | **0.68** | A 已强，博弈维持                  |
| BAS    | 66     | 0.68     | 0.17     | 0.41     | 0.61     | B/C 反而拖累，博弈略降            |
| ABE    | 1      | 0.00     | 0.00     | 0.00     | 0.00     | 测试集仅 1 样本，无法评估         |

**关键发现**：LYI 和 FGC 这两个最极端的稀有类（分别仅 10 和 7 个测试样本），Expert A 完全无法识别（F1=0），**只有博弈集成才能检测到**（F1=0.60）。这是因为 Expert B 和 C 虽然单独表现差，但对这两个类有微弱但非零的信号，博弈机制将这些信号放大融合。

### 5.3 与 MLL 数据集已有文献的对比

以下仅列出在**同一 MLL 数据集**（171k 图像，21 类）上报告结果的论文：

| 方法         | 年份     | 骨干       | 参数量   | Weighted F1 | Macro F1    |
| ------------ | -------- | ---------- | -------- | ----------- | ----------- |
| Matek et al. | 2021     | ResNeXt    | ~25M     | -           | 0.56 (test) |
| DinoBloom    | 2024     | DINOv2-G   | **1.1B** | 85.7%       | -           |
| CytoDINO     | 2024     | DINOv3     | **~1B**  | **88.2%**   | **76.5%**   |
| **Ours**     | **2026** | **Swin-T** | **28M**  | **86.3%**   | **71.5%**   |

- Weighted F1 超过了 DinoBloom 在 MLL 上的结果（85.7% → 86.3%）
- 与 CytoDINO 的差距（Macro F1 差 5 个点）主要来源于骨干参数量差距（28M vs ~1B，差 40 倍）
- 我们的框架与骨干网络正交，可以迁移至更强的基础模型

注：DinoBloom 常被引用的 89.1% Weighted F1 是在 Acevedo 数据集上的结果，**不是 MLL 数据集**。SCKansformer 的 83.23% Accuracy 是在 BMCD-FGCD 数据集（40 类）上的结果，同样不可直接对比。

---

## 六、方案迭代总结

| 对比项                  | 原方案（3.25 汇报）    | v1 三阶段路由      | v2 成本非对称竞标         |
| ----------------------- | ---------------------- | ------------------ | ------------------------- |
| 骨干网络                | ResNet50 (冻结)        | Swin-Tiny          | Swin-Tiny                 |
| 分类架构                | 21 独立专家 MoE        | 3 专家 + 门控路由  | 3 专家 + 非对称成本竞标   |
| 路由机制                | softmax 出价税         | 对称门控           | **非对称成本编码专家优先级** |
| 与 RIDE/SADE 的区别     | 不同                   | 类似（对称门控）   | **专家成本非对称（核心创新）** |
| 与 MoB 的区别           | -                      | -                  | **软聚合 + 外生成本（vs MoB 硬选择 + VCG）** |
| Macro F1 (test)         | 0.397 / 0.612          | 0.682              | **0.715**                 |
| Tail F1 (test)          | ~0                     | 0.436              | **0.545**                 |
| 关键消融证据            | -                      | -                  | **vs CE Gating: +12.5% Tail F1** |

---

## 七、下一步计划

### 投稿目标：ICONIP 2026

### 需要完成的工作

1. **补充基线对比方法**（覆盖 2017-2024）
   - **经典**: LDAM-DRW (NeurIPS 2019), Logit Adjustment (ICLR 2021)
   - **多专家**: RIDE (ICLR 2021), **SADE (NeurIPS 2024)** ← 直接竞争对手
   - **2024 新基线**: GALA (Gradient-Aware Logit Adjustment, 2024)
   - **覆盖最新拍卖类工作**: 在 related work 详细对比 MoB (Mixture of Bidders, 2024.12)

2. **核心消融实验（重点）**

   - **路由策略消融**（已有数据，确认即可）：Simple Average / CE Gating / Bidding Game
   - **成本机制消融**（论证非对称性的必要性）：
     * 对称低成本 $(0.1, 0.1, 0.1)$
     * 对称中等 $(0.5, 0.5, 0.5)$
     * 对称高成本 $(1.0, 1.0, 1.0)$
     * **反向** $(1.0, 0.5, 0.1)$
     * **可学习成本**（$c_i$ 作为参数与网络一起学习）
     * **当前** $(0.1, 0.5, 1.0)$
   - $\alpha$ 系数敏感性（含 $\alpha=0$ 退化为 CE 门控）
   - 专家组合消融（A/B/C 任意子集）
   - ResNet50 骨干消融（证明方法骨干无关）

3. **可视化（医学论文标配）**

   - **Grad-CAM 三专家注意力对比**：在同一细胞图上展示三专家关注区域的差异
   - **出价分布散点图**：验证训练后的出价是否符合 $b_i^* \propto 1/c_i$ 的理论预测
   - **专家分配权重热力图**：每类样本路由到哪个专家
   - **混淆矩阵**：21 类错分模式
   - **t-SNE**：三专家学到的特征空间结构

4. **论文撰写**

   - 论文定位：**成本非对称的多专家路由**用于长尾医学分类
   - 主卖点：实证证据（vs CE Gating +12.5% Tail F1）+ 医学会诊机制的形式化 + 可解释的决策路径
   - 核心创新点的"防御性表述"：
     * 不强调"我们用 Nash 均衡训练"（避免审稿人质疑闭式解未被强制约束）
     * 强调"非对称成本编码专家优先级"（确认无先例）
     * 弱化"博弈论"包装（避免与 MoB 直接对撞），强调"机制设计 + 多专家集成"
   - 论文定位：两阶段框架（表征学习 + 竞标博弈），核心贡献在博弈机制
   - 论文叙事：单一损失函数无法同时优化头部和尾部（消融证明）→ 多专家竞标博弈自适应分配
     ssh -p 48330 root@connect.westd.seetacloud.com
     ssh -p 48330 root@connect.westd.seetacloud.com
