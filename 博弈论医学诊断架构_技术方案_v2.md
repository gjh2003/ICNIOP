# 基于博弈论的骨髓细胞长尾分类框架 —— 技术方案文档

## 一、研究背景与问题定义

### 1.1 任务描述

骨髓细胞形态学分类是血液病理诊断的基础。本研究基于 MLL（Munich Leukemia Laboratory）骨髓细胞数据集，目标是将细胞图像分为 **21 种造血谱系亚型**。

### 1.2 核心挑战：极端长尾分布

数据集包含 **171,374 张**专家标注的细胞图像，类别分布极度不均衡：

| 类别 | 样本数 | 占比 | 分组 |
|------|--------|------|------|
| NGS (成熟中性粒细胞) | 29,424 | 17.17% | Head |
| EBO (晚幼红细胞) | 27,395 | 15.99% | Head |
| LYT (成熟淋巴细胞) | 26,242 | 15.31% | Head |
| ART (伪影/杂质) | 19,630 | 11.45% | Head |
| PMO (早幼粒细胞) | 11,994 | 7.00% | Body |
| BLA (原始细胞) | 11,973 | 6.99% | Body |
| ... | ... | ... | Body |
| BAS (嗜碱性粒细胞) | 441 | 0.26% | Tail |
| HAC (毛细胞) | 409 | 0.24% | Tail |
| OTH (其他) | 294 | 0.17% | Tail |
| LYI (不典型淋巴细胞) | 65 | 0.04% | Tail |
| FGC (Faggot细胞) | 47 | 0.03% | Tail |
| KSC (破碎细胞) | 42 | 0.02% | Tail |
| ABE (异常嗜酸性粒细胞) | 8 | 0.005% | Tail |

**最大类与最小类比例高达 3,678:1**，属于极端长尾分布。

### 1.3 现有方法的不足

| 主流方法 | 缺点 |
|----------|------|
| 重采样/GAN生成少数类 | 破坏真实分布，跨域泛化差 |
| Focal Loss / 加权CE | 仅在损失函数层面调整，无法从根本上改变特征空间结构 |
| 多示例学习(MIL)预测基因突变 | 放弃单细胞级解释性，成为黑盒 |
| 大模型暴力刷榜(ViT/ConvNeXt) | 纯算力堆砌，缺乏数学与医学insight |

---

## 二、理论框架：非对称纳什竞标博弈

### 2.1 博弈公式化

将分类过程建模为非合作多智能体博弈 $\mathcal{G} = \langle \mathcal{N}, \mathcal{S}, \mathcal{U} \rangle$：

- **参与者** $\mathcal{N}$：$K=21$ 个独立专家智能体，各负责一个造血谱系
- **策略空间** $\mathcal{S}$：专家 $k$ 对输入 $x$ 给出出价 $b_k \in \mathbb{R}$
- **分配机制**：Softmax 将出价转换为归属概率

$$p_k(\mathbf{b}) = \frac{\exp(b_k)}{\sum_{j=1}^K \exp(b_j)}$$

### 2.2 阶级固化出价税

为专家 $k$ 定义非对称效用函数：

$$U_k(b_k, \mathbf{b}_{-k}) = \mathcal{R}_k(x) \log p_k(\mathbf{b}) - \frac{\tau}{2} \cdot \pi_k \cdot b_k^2$$

其中：
- $\mathcal{R}_k(x) \in \{0, 1\}$：ground truth 匹配（训练时为 one-hot）
- $\pi_k$：类别先验频率
- $\tau$：系统税率系数
- **核心机制**：$\pi_k$ 越大（大类），出价的边际成本越高

### 2.3 纳什均衡解

对效用函数求导令其为零，得到均衡出价：

$$b_k^* = \frac{\mathcal{R}_k(x) \cdot (1 - p_k(\mathbf{b}))}{\tau \cdot \pi_k}$$

**关键性质**：均衡出价与 $\pi_k$ 严格反比。大类被高税压制，小类获免税补贴。

### 2.4 训练损失推导

从负总效用推导训练损失：

$$\mathcal{L} = -\log p_y + \frac{\tau}{2} \sum_{k=1}^{K} \pi_k \cdot b_k^2 = \text{CE}(\mathbf{b}, y) + \frac{\tau}{2} \sum_k \pi_k \cdot b_k^2$$

即：**交叉熵 + 按类别先验加权的 logit L2 正则化**。

---

## 三、实验迭代与发现

### 3.1 实验一：冻结特征 + Nash 税（失败）

**设置**：使用 ImageNet 预训练的 ResNet50 提取 2048 维特征（冻结），在此基础上训练分类器。

| 方法 | Accuracy | Macro F1 |
|------|----------|----------|
| Standard CE | 68.60% | 0.388 |
| Nash Bidding (τ=1.0) | 67.37% | 0.374 |
| Nash Bidding (τ=0.001) | 68.60% | 0.397 |
| Weighted CE | 57.80% | 0.393 |
| Focal Loss (γ=2) | 56.86% | 0.404 |

**发现**：
1. **Nash 税 τ=1.0 时反而降低了性能**，因为对大类惩罚过重
2. 通过 τ 消融实验发现 **τ=0.001 是最佳点**，Macro F1 从 0.388 提升至 0.397（+2.3%）
3. **所有方法的天花板都很低**（~68% Acc），根本原因是 ImageNet 预训练特征对骨髓细胞域适配性差

**结论**：冻结特征条件下，损失函数层面的调整效果有限，必须端到端微调。

### 3.2 实验二：重训练 ResNet50

**设置**：ResNet50 骨干网络端到端训练，差异化学习率（backbone 1e-5, head 1e-3），10 个 epoch。

| 方法 | Accuracy | Macro F1 | Weighted F1 | Head F1 | Tail F1 |
|------|----------|----------|-------------|---------|---------|
| Standard CE | **85.98%** | 0.6072 | 0.8575 | 0.9110 | 0.2305 |
| Weighted CE | 83.21% | **0.6158** | 0.8388 | 0.8901 | **0.2936** |
| Focal Loss (γ=2) | 72.45% | 0.5172 | 0.7508 | 0.8052 | 0.1834 |
| Nash Bidding (τ=0.001) | **86.14%** | 0.6122 | **0.8588** | **0.9124** | 0.2455 |

**关键发现**：

1. **端到端微调带来巨大提升**：准确率从 68.6% 跃升至 86.1%（+17.5%），证实了特征适配是核心瓶颈
2. **Nash Bidding 在 Accuracy 和 Head F1 上最优**：86.14% Acc，0.912 Head F1
3. **Weighted CE 在 Macro F1 和 Tail F1 上最优**：0.616 Macro F1，0.294 Tail F1
4. **Focal Loss 效果最差**：γ=2 的聚焦效应在此数据集上导致头部类崩溃（Acc 仅 72%）
5. **7 个尾部类的 F1 仍为 0**（ABE/BAS/FGC/HAC/KSC/LYI/OTH），所有方法均无法识别极端稀有类

**结论**：Nash 税在维持头部性能的同时提供了一定的尾部改善，但单一损失函数的调整不足以解决 3678:1 的极端长尾问题。

---

## 四、改进方案：三阶段纳什博弈框架

基于实验发现和文献调研（Decoupled Training, Logit Adjustment, SADE Multi-Expert Ensemble），提出以下改进方案。

### 4.1 总体架构

```
┌─────────────────────────────────────────────────────┐
│  Stage 1: 表征学习 (Representation Learning)         │
│  ├─ 标准 CE，端到端训练 ResNet50                      │
│  └─ 目标：学习高质量的骨髓细胞特征空间                   │
├─────────────────────────────────────────────────────┤
│  Stage 2: 纳什均衡分类器 (Nash Equilibrium Classifier) │
│  ├─ 冻结骨干网络                                      │
│  ├─ Logit Adjustment：logit_k' = logit_k + τ·log(π_k)│
│  ├─ Nash 出价税：(τ/2)·Σ π_k·b_k²                    │
│  └─ 目标：在已有特征上精调决策边界                       │
├─────────────────────────────────────────────────────┤
│  Stage 3: 多专家纳什路由 (Multi-Expert Nash Routing)   │
│  ├─ Expert A：标准 CE（头部专精）                       │
│  ├─ Expert B：加权 CE + 均衡采样（尾部专精）             │
│  ├─ Expert C：Logit Adjustment + Nash 税（极端尾部）    │
│  ├─ Nash Router：学习最优专家分配权重                    │
│  └─ 目标：多智能体博弈实现自适应分类                      │
└─────────────────────────────────────────────────────┘
```

### 4.2 Stage 1：表征学习

**理论依据**：Decoupled Training（Kang et al., ICLR 2020）证明数据不平衡主要损害分类器，而非特征表征。先用自然分布训练骨干网络可以学到最好的特征。

- 模型：ResNet50（ImageNet 预训练）+ MLP 分类头
- 损失：标准 CrossEntropyLoss
- 优化器：AdamW，差异化学习率（backbone 1e-5, head 1e-3）
- 训练：5 个 epoch

### 4.3 Stage 2：纳什均衡分类器

**理论依据**：Logit Adjustment（Menon et al., ICLR 2021）通过 $\log(\pi_k)$ 偏移 logit，提供贝叶斯一致的决策边界校正。结合 Nash 出价税实现双重调节。

**冻结骨干网络**，仅训练新的分类头。损失函数：

$$\mathcal{L}_{S2} = \underbrace{-\log\left(\text{softmax}\left(\mathbf{b} + \tau_{LA} \cdot \log \boldsymbol{\pi}\right)_y\right)}_{\text{Logit-Adjusted CE}} + \lambda_{tax} \cdot \underbrace{\frac{\tau_{Nash}}{2} \sum_{k} \pi_k \cdot b_k^2}_{\text{Nash 出价税}}$$

各项作用：
- **Logit Adjustment**：直接移动决策边界，给尾部类更大的margin
- **Nash 出价税**：在调整后的边界上进一步约束大类的出价幅度
- 两者互补：LA 负责"移动边界"，Nash 负责"约束力度"

### 4.4 Stage 3：多专家纳什路由

**理论依据**：SADE（NeurIPS 2024）证明多专家集成是当前长尾分类 SOTA。本方案将其与博弈论路由机制结合。

**三个专家**（冻结骨干，各自独立训练）：

| 专家 | 训练方式 | 角色 |
|------|---------|------|
| Expert A | 标准 CE + 自然分布 | 头部类专精，保证大类准确率 |
| Expert B | 加权 CE + 均衡采样 | 尾部类专精，通过过采样增强稀有类 |
| Expert C | Logit Adjustment + Nash 税 | 极端尾部关注，强 LA 偏移 |

**Nash 路由网络**：
- 输入：2048 维特征向量
- 输出：3 个专家的分配权重 $\mathbf{w} = \text{softmax}(f_{router}(\mathbf{x}))$
- 最终预测：$\hat{y} = \arg\max \sum_{i=1}^{3} w_i \cdot \text{logits}_i$
- 训练目标：最小化加权集成后的交叉熵

**博弈论解释**：三个专家是三个具有不同偏好（头部/尾部/极端尾部）的竞标者，路由网络是拍卖机制，根据输入样本的特征将"分类权"分配给最合适的专家。这比单一损失函数的 Nash 税构成了更丰富的多智能体博弈结构。

**分配策略细分一下，不能直接选择权重最大的专家**(比如A＞B＋C选A；从数学上做文章；K近邻)

---

## 五、实验设计

### 5.1 数据划分

- 划分比例：70% / 20% / 10%（训练/验证/测试）
- 划分方式：分层采样（stratified），保证每个类在每个子集中有代表
- 类别先验 $\pi_k$：仅从训练集计算（Laplace 平滑）

### 5.2 评估指标

| 指标 | 说明 | 重要性 |
|------|------|--------|
| Accuracy | 整体准确率 | 受大类主导，参考价值有限 |
| Macro F1 | 21 类 F1 等权平均 | **主要指标**，最能反映长尾问题 |
| Weighted F1 | 按样本量加权 F1 | 反映整体用户体验 |
| Head F1 | π_k > 0.10 的 4 类平均 F1 | 头部类表现 |
| Tail F1 | π_k < 0.005 的 7 类平均 F1 | **关键指标**，检验尾部改善 |

### 5.3 对比方法

| 方法 | 说明 |
|------|------|
| Standard CE | 基线，无任何长尾处理 |
| Weighted CE | 按频率反比加权的交叉熵 |
| Focal Loss (γ=2) | 聚焦困难样本的损失函数 |
| Nash Bidding (单阶段) | 本研究的初始方案 |
| **三阶段 Nash 框架** | **本研究的改进方案** |

---

## 六、代码结构

```
c:\Desktop\code_gjh\
├── BMC_dataset/MLL/bone_marrow_cell_dataset/   # MLL 数据集 (21 类)
├── saved_features/                              # ResNet50 预提取特征
│   ├── mll_features_resnet50.npy               # (171374, 2048)
│   ├── mll_labels.npy                          # (171374,)
│   └── mll_class_mapping.npy                   # 类别名映射
├── nash_bidding/                                # 核心算法包
│   ├── config.py                               # 超参数配置
│   ├── data.py                                 # 数据加载与划分
│   ├── model.py                                # NashBiddingClassifier (21 独立专家)
│   ├── loss.py                                 # NashBiddingLoss / FocalLoss / WrappedCELoss
│   ├── train.py                                # 训练循环
│   ├── evaluate.py                             # 评估与纳什均衡分析
│   ├── baselines.py                            # 基线对比
│   ├── ablation.py                             # τ 消融实验
│   ├── finetune_model.py                       # 端到端微调模型
│   ├── finetune_data.py                        # 图像数据管道
│   ├── finetune_train.py                       # 端到端训练脚本
│   └── run_all.py                              # 命令行入口
├── run_experiment.py                            # 单阶段基线对比实验
├── run_improved.py                              # 三阶段改进方案实验
├── checkpoints/                                 # 模型权重
└── results/                                     # 实验结果与图表
```

---

## 七、已有实验结果

### 7.1 冻结特征实验（ResNet50 预提取，不微调）

| 方法 | Accuracy | Macro F1 |
|------|----------|----------|
| Standard CE | 68.60% | 0.388 |
| Nash (τ=0.001) | 68.60% | **0.397** |
| Focal Loss | 56.86% | 0.404 |

**τ 消融实验**（τ 从 0 到 10）发现 τ=0.001 为最优，但提升幅度有限。

### 7.2 端到端微调实验（ResNet50 可训练，5 epoch）

| 方法 | Accuracy | Macro F1 | Weighted F1 | Head F1 | Tail F1 |
|------|----------|----------|-------------|---------|---------|
| Standard CE | 85.98% | 0.607 | 0.858 | 0.911 | 0.231 |
| Weighted CE | 83.21% | **0.616** | 0.839 | 0.890 | **0.294** |
| Focal Loss | 72.45% | 0.517 | 0.751 | 0.805 | 0.183 |
| Nash (τ=0.001) | **86.14%** | 0.612 | **0.859** | **0.912** | 0.246 |

### 7.3 三阶段改进方案

代码已实现（`run_improved.py`），待运行。

---

## 八、下一步计划

1. **运行三阶段实验**：执行 `python run_improved.py`，获取完整对比数据
2. **纳什均衡验证**：分析 Stage 3 中各专家的出价分布，验证均衡理论预测
3. **消融实验**：对 Stage 2 的 τ_LA 和 τ_Nash，以及 Stage 3 的专家组合进行消融
4. **可视化**：混淆矩阵、per-class F1 对比柱状图、bid 与 1/π_k 的相关性散点图

---

## 参考文献

1. Kang et al., "Decoupling Representation and Classifier for Long-Tailed Recognition", ICLR 2020
2. Menon et al., "Long-tail Learning via Logit Adjustment", ICLR 2021
3. Lin et al., "Focal Loss for Dense Object Detection", ICCV 2017
4. SADE, "Breaking Long-Tailed Learning Bottlenecks", NeurIPS 2024
5. Matek et al., "Highly accurate differentiation of bone marrow cell morphologies using deep neural networks", Blood 2021
