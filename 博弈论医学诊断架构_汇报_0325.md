## 存在的问题

![image-20260324113117377](C:\Users\26353\AppData\Roaming\Typora\typora-user-images\image-20260324113117377.png)

![image-20260324113139609](C:\Users\26353\AppData\Roaming\Typora\typora-user-images\image-20260324113139609.png)

- 过多的超参数 专家难以收敛

在原有的 DPMM + 博弈论框架中，系统耦合了过多相互制约的超参数：中餐厅过程的浓度参数 $\alpha$、裂变阈值 $\tau$、博弈出价成本 $\lambda$、EMA 的动量衰减率等。 在数学理论上，当一个动态系统的优化流形受到过多高维超参数的非线性约束时，系统会陷入混沌边缘。

- “语义鸿沟”导致的分类与聚类错位 

DPMM 是一种纯粹的无监督密度估计器，它的本质是寻找高维空间中的“高斯聚集簇”。 然而，医学图像的分类（如 MLL 的 21 个造血谱系）是经过人类上百年病理学沉淀总结出的高级语义分类学。数学上的“密度聚类”和医学上的“语义分类”之间存在着不可跨越的语义鸿沟。 如果放任模型去无监督裂变，它极大概率会按照细胞的“染色深浅”、“物理大小”甚至“背景玻片杂质”去裂变出 50 个专家，而绝不会乖乖地按照“红系、粒系、淋巴系”这种人类医学常识去聚类。试图用底层的聚类逻辑去强行倒推顶层的医学金标准，在理论上是本末倒置的。

## MLL 数据集展开的工作

### 1.强行解决“极端长尾分布”

骨髓数据集中，成熟分叶核粒细胞占比极大，而某些变异细胞极少。

![image-20260324110020129](C:\Users\26353\AppData\Roaming\Typora\typora-user-images\image-20260324110020129.png)

- **主流做法**：研究者大量使用重采样技术（如 Embedding-Space Re-sampling）、生成对抗网络（GAN）或者复杂的 Focal Loss 变体，强行生成少数类样本或修改损失权重。
- **缺点**：这些方法全是“人工补丁”，不仅破坏了原始数据的真实分布，而且在面临新的跨院区数据时极易崩溃。

### 2. 从细胞分类走向“基因突变预测 (Genotype Prediction)”

- **主流做法**：最新的 Nature Communications 和 arXiv 预印本文章，开始利用多示例学习 (MIL)。他们把 MLL 训练好的模型作为一个特征提取器，不去纠结单个细胞分类对不对，而是把几百个细胞打包，直接预测病人是否患有 AML 及其特定基因突变（如 NPM1）。
- **缺点**：这种做法完全放弃了对骨髓细胞“微观发育过程”的解释，模型变成了一个彻底的黑盒。

### 3. 引入大模型架构与暴力刷榜

- **主流做法**：MICCAI 上的近期论文，开始把各种魔改的 Vision Transformers (ViT)、ConvNeXt、甚至神经细胞自动机 (Neural Cellular Automata) 套用在 MLL 上，试图把 21 分类的准确率再往上挤 0.5%。
- **缺点**：纯纯的算力堆砌，缺乏深度的医学和数学 insight。



## 算法改进

### 1 非对称纳什竞标博弈公式化 

我们将骨髓流形空间中的分类过程，建模为一个具有资源约束的非合作多智能体博弈 $\mathcal{G} = \langle \mathcal{N}, \mathcal{S}, \mathcal{U} \rangle$。

- **参与者 (Players) $\mathcal{N}$**: $K$ 个负责不同造血谱系的独立专家智能体（在本研究中 $K=21$）。每个类别在自然界（训练集）中的先验频率记为 $\pi_k \in (0, 1)$，且满足 $\sum_{k=1}^K \pi_k = 1$。

- **策略空间 (Strategies) $\mathcal{S}$**: 面对输入细胞的视觉特征 $x$，每个专家 $k$ 独立给出一个竞标出价（Bid Logit） $b_k \in \mathbb{R}$。全场专家的出价向量记为 $\mathbf{b} = [b_1, b_2, ..., b_K]$。

- **分配机制 (Allocation Mechanism)**: 路由系统根据出价，通过 Softmax 函数分配样本的归属概率（署名权）：

  $$p_k(\mathbf{b}) = \frac{\exp(b_k)}{\sum_{j=1}^K \exp(b_j)}$$

### 2 阶级固化出价税与效用函数 

传统分类网络（交叉熵）的本质是“零和博弈”，这不可避免地导致大类样本的梯度霸权。为内生化解长尾效应，我们为专家 $k$ 引入了独立且非对称的期望效用函数 $U_k$：

$$U_k(b_k, \mathbf{b}_{-k}) = \mathcal{R}_k(x) \log p_k(\mathbf{b}) - \mathcal{C}_k(b_k)$$

**【公式物理意义解析】**

1. **期望收益项 $\mathcal{R}_k(x) \log p_k(\mathbf{b})$**：其中 $\mathcal{R}_k(x) \in [0, 1]$ 代表输入特征 $x$ 属于谱系 $k$ 的真实生物学后验概率（Ground Truth Match）。专家竞标获得的概率 $p_k$ 越大，收益越高。

2. **纳什出价成本 $\mathcal{C}_k(b_k)$**：我们首次定义了**基于类别先验频率的阶级固化出价税**：

   $$\mathcal{C}_k(b_k) = \frac{\tau}{2} \cdot \pi_k \cdot b_k^2$$

   其中 $\tau$ 为系统税率调节系数。该惩罚项的核心在于：**专家的先验频率 $\pi_k$ 越大，其出价的边际成本越高。**

### 3 求解纳什均衡：内生化解长尾霸权

在训练过程中，系统处于非合作博弈状态，每个专家均寻求自身效用的最大化，即 $\arg\max_{b_k} U_k$。为求解该博弈的纳什均衡点（Nash Equilibrium），我们对效用函数求偏导并令其为零：

$$\frac{\partial U_k}{\partial b_k} = \mathcal{R}_k(x) \cdot \frac{\partial \log p_k}{\partial b_k} - \tau \cdot \pi_k \cdot b_k = 0$$

代入 Softmax 导数性质 $\frac{\partial \log p_k}{\partial b_k} = 1 - p_k(\mathbf{b})$，我们可以得到纳什均衡状态下专家的最优出价策略 $b_k^*$：

$$b_k^* = \frac{\mathcal{R}_k(x) \cdot (1 - p_k(\mathbf{b}))}{\tau \cdot \pi_k}$$

**【对长尾分布的理论绝杀】**

上式是本框架的灵魂。在均衡点 $b_k^*$ 处，出价与先验频率 $\pi_k$ 呈**严格反比关系**。这意味着：

- 对于成熟中性粒细胞等**绝对大类（$\pi_k$ 极大）**，高昂的纳什边际成本死死压制了其出价 $b_k^*$，剥夺了其利用数量优势对决策边界的过度侵占。
- 对于原始病变细胞等**极少数类（$\pi_k \to 0$）**，系统给予了天然的“免税补贴”，使其只需微弱的形态学匹配度 $\mathcal{R}_k(x)$ 即可给力出价，极大地增强了流形边缘的召回率。长尾问题在此数学架构下被自发消解。

